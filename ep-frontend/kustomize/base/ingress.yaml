apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ndp-ep-frontend-ingress
spec:
  ingressClassName: placeholder-class # patched per overlay
  rules:
    - host: placeholder.example.com # patched per overlay
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ndp-ep-frontend-service
                port:
                  number: 3000
